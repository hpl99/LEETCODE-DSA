int reverse(int x){
        int a =0;
        int y =0;
    int original = x;
    if(x<0){
        if(x==INT_MIN){
            return 0;
        }
        x = abs(x);
          while(x!=0){
            a = x%10;
            if (y > INT_MAX  / 10) 
            {
            return 0;
        }
        y = y*10+a;
            x = x/10;
        }
        return -y;
    }
    else if(x>0){
        while(x!=0){
            a = x%10;
            if (y > (INT_MAX - a) / 10) 
            {
            return 0;
        }
        y = y*10+a;
            x = x/10;
        }
        
    return y;
}
else{
    return x;
}
}